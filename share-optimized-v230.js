// V2.3.2 — Compatibilidade do módulo antigo de compartilhamento.
// O fluxo oficial agora está em share-config.js e usa código de 12 caracteres.
// Este arquivo não cria modal, não pede JSON e não registra listeners de compartilhamento.
(() => {
  'use strict';
  window.__mlShareOptimizedLoaded = true;
})();
